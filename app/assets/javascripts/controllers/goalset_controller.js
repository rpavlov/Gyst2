App.GoalsetController = Em.ObjectController.extend({
/*  isEditing: false,

  startEditing: function() {
    var goalsetEditController = this.controllerFor('goalsetEdit');
    goalsetEditController.set('content', this.get('content'));
    goalsetEditController.startEditing();
    this.set('isEditing', true);
  },

  stopEditing: function() {
    var goalsetEditController = this.controllerFor('goalsetEdit');
    goalsetEditController.stopEditing();
    this.set('isEditing', false);
  },

  destroyRecord: function() {
    if (window.confirm("Are you sure you want to delete this Goalset?")) {
      this.get('content').deleteRecord();
      this.get('store').commit();

      // return to the main contacts listing page
      this.get('target.router').transitionTo('goalsets.index');
    }
  }*/
});